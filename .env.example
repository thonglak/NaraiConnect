# Root .env — used only for docker-compose variable substitution.
# Application config (DB, OAuth, session, etc.) lives in apps/admin/.env
# Copy apps/admin/.env.example → apps/admin/.env and edit there.

ADMIN_PORT=8000
